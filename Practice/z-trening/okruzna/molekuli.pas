begin
  readln(n);
  for i:=1 to 3 do
    begin
      readln(niz[i]);
      delete(niz[i],1,1);
      delete(niz[i],length(niz[i]),1);
    end;
  dec(n,2);
  for i:=1 to 4 do
    for j:=1 to 4 do
      for k:=1 to 4 do
        for m:=1 to 4 do
          if
            then
end.